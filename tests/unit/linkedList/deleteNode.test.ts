import { ListNode } from "../../../src/utils/linked-list";
import deleteNode from "../../../src/utils/linked-list/deleteNode";

test('deleteNode', ()=>{
    // const d = new ListNode(9, null)
    // const c = new ListNode(1, d)
    // const b = new ListNode(5, c)
    // const a = new ListNode(4, b)
    // expect(deleteNode(a)).toEqual(b)
})
